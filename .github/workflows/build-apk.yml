
امروز ۱۱:۰۰

project_ahanonline_marketer_wheel.zip
فشرده‌سازی بایگانی
کامبوباکس انتخابگر چرخشی را دو ردیفه کن ، در ضمن برای کامبوکاس طرف حساب * (مشتری) هم همین کامبوباکس  چرخشی دو ردیفه تولید کن

5ba6470d-08cf-4ce8-95eb-64686da9aabd.png

عرض این کارتها را کم کن و دو کارت دیگر به این قسمت اضافه کن
گزارش کالاهای ورودی تسویه‌نشده
تقویم ارسال فاکتورها

583e403e-6744-49c8-9a9a-dc9f178b2864.png
قیمت ورق گالوانیزه را از این سطر بگیر ضخامت 0.8 
عرض 1250
برند : هفت الماس
محل تحویل : بنگاه تهران 
واحد : کیلوگرم

به‌مدت 3m 10s پردازش کرد
ادامه داده شد و تغییر کامل شد.

قیمت ورق گالوانیزه فقط از ردیف ضخامت ۰٫۸، عرض ۱۲۵۰، برند هفت الماس، بنگاه تهران، واحد کیلوگرم خوانده می‌شود.

قیمت به‌صورت پویا از آهن‌آنلاین دریافت می‌شود و ثابت نیست.

هنگام بررسی، قیمت همین ردیف ۱۳۷٬۰۰۰ تومان بود. 

عنوان مرجع قیمت در محاسبه خودکار خرید نیز اصلاح شد.

تاریخچه قیمت ردیف قبلی شهرکرد با ردیف جدید ترکیب نمی‌شود.

کش مرورگر و PWA به‌روزرسانی شد.

بررسی Python و JavaScript موفق بود و آزمون نهایی ۴۸ از ۴۸ گذشت.

دانلود پروژه اصلاح‌شده


c05b391a-0cdb-4f44-a6f4-397010fea634.png
در قسمت قیمت ورق سیاه  ضخامت 2 میل نیز اصلاحات زیر انجام شود 
از آدرس 
https://ahanonline.com/product-category/%D8%A7%D9%86%D9%88%D8%A7%D8%B9-%D9%88%D8%B1%D9%82/%D9%88%D8%B1%D9%82-%D8%B3%DB%8C%D8%A7%D9%87/
کادر ورق سیاه 2 میل
سایز : 1000*2000 
برند : فولاد مبارکه
حالت : برش خورده
محل تحویل : بتگاه تهران 


ragham_web_invoice_prices_view_fixed.zip
فشرده‌سازی بایگانی
آیا امکان نصب این برنامه روی سرور هست و آیا هر کاربر میتواند با کدی که در اختیارش قرار میگیرد به برنامه دسترسی داشته باشد ، آیا امکان تعریف سطح دسترسی داریم ، آیا زمانیکه کاربر داده ای را وارد میکند امکان مشاهده تغییرات توسط مدیر هست ؟ فقط جواب بده 

6aec1734-c8c5-4792-8e7f-0bb87a6f8602.png


project_ahanonline_black_sheet_2mm_1000x2000_mobarakeh_cut_tehran(1).zip
فشرده‌سازی بایگانی
یک فایل جیسون برای من تولید کن که در آن دسته ، نام کالا و آخرین  قیمت  خرید آن کالا در آن موجود باشد


ragham_web_admin_users_permissions(1).zip
فشرده‌سازی بایگانی
دسته ، نام کالا و آخرین قیمت خرید را از این فایل استخراج و در انبار نمایش بده

b24f6e7a-b11b-44a2-8ef4-3aac5264c7d9.png
رنگ دکمه  باعث شده  متن خوانده نشود ، رنگ دکمه را ملایم کن

e0c9dfde-633d-460e-b54e-11eeebe09ce4.png
 این رنگ دکمه حذف را هم  مثل قبلی اصلاح کن

83eb3dbe-a074-4cc4-b632-8a6bd12791e6.png
در فرم مشاهده فاکتور خرید ، ارتفاع این کادر به حداقل برشد و داده های درون آن در یک سطر قرار بگیرند

به‌مدت 8m 16s پردازش کرد
نسخه سروری مرحله اول آماده شد.

امکانات این نسخه:

اجرا روی Linux، Windows Server و Docker

ذخیره مرکزی اطلاعات در SQLite

همگام‌سازی فاکتورها، تراکنش‌ها، کاربران، انبار و تاریخچه تغییرات بین مرورگرها

کنترل تعارض هنگام ذخیره هم‌زمان

بروزرسانی خودکار اطلاعات هر ۸ ثانیه

ابزار تهیه نسخه پشتیبان از پایگاه داده

امکان انتقال فایل JSON قبلی به سرور

فایل نمونه تنظیم Nginx، HTTPS و systemd

امکان فعال‌کردن نام کاربری و رمز کلی روی کل سایت

برای اجرای سریع:

ویندوز: start_windows.bat

لینوکس: ./start_linux.sh

Docker: docker compose up -d --build

آدرس پیش‌فرض:

http://IP-SERVER:8080
این نسخه داده‌ها را واقعاً روی سرور نگهداری می‌کند؛ اما احراز هویت کاربران و کنترل دسترسی کاملاً سمت سرور، به‌عنوان مرحله امنیتی بعدی باقی مانده است. تا آن مرحله بهتر است برنامه پشت HTTPS، VPN یا Basic Auth اجرا شود.

دانلود نسخه سروری نرم‌افزار رقم



ragham_web_invoice_view_compact.zip
فشرده‌سازی بایگانی
امکان ذخیره فایل پشتیبان در قالب JSON و همچنین بازیابی آن برای مدیر فراهم کن

به‌مدت 6m 47s پردازش کرد
امکان پشتیبان‌گیری و بازیابی JSON به نسخه سروری نرم‌افزار رقم اضافه شد.

دکمه «ذخیره پشتیبان JSON» فقط برای مدیر نمایش داده می‌شود.

فایل پشتیبان شامل کالاها، کاربران، فاکتورها، تراکنش‌ها، گردش انبار، تنظیمات و تاریخچه اصلاحات است.

مدیر می‌تواند فایل JSON را انتخاب و اطلاعات را روی پایگاه داده سرور بازیابی کند.

پیش از بازیابی، تعداد رکوردهای فایل نمایش داده شده و تأیید مدیر دریافت می‌شود.

قبل از جایگزینی اطلاعات، یک فایل پشتیبان خودکار از وضعیت فعلی دانلود می‌شود.

نسخه قبلی اطلاعات نیز داخل پایگاه داده SQLite سرور نگهداری می‌شود.

فایل‌های JSON قدیمی برنامه هم قابل بازیابی هستند.

بررسی نحوی JavaScript و Python و آزمون بازیابی روی SQLite موفق بود.

دانلود نسخه سروری ۱.۱ با پشتیبان‌گیری و بازیابی JSON


چگونه باید نسخه سرور را راه اندازی و اجرا کنم ، من مبتدی هستک

امروز ۱۷:۲۳
به‌مدت 9m 28s پردازش کرد
نسخه اندرویدی نرم‌افزار رقم آماده شد.

این نسخه:

به نسخه سروری برنامه متصل می‌شود.

در اولین اجرا آدرس سرور را درخواست می‌کند.

امکان تغییر آدرس سرور از منوی برنامه دارد.

ورود مدیر و کاربران را نمایش می‌دهد.

دانلود فایل پشتیبان JSON و انتخاب فایل برای بازیابی را پشتیبانی می‌کند.

گزارش‌ها را از طریق چاپ اندروید به PDF تبدیل می‌کند.

دکمه بازگشت، تازه‌سازی و خروج از حساب دارد.

برای ساخت خودکار APK در GitHub، فایل GitHub Actions نیز داخل پروژه قرار گرفته است.

در این محیط امکان دانلود Android SDK و تولید مستقیم APK فراهم نشد؛ بنابراین فعلاً پروژه کامل Android Studio تحویل داده شده است. با بارگذاری آن در GitHub و اجرای گردش‌کار Build Android APK، فایل APK ساخته می‌شود.

دانلود پروژه نسخه اندرویدی نرم‌افزار رقم

نسخه سرور موردنیاز برنامه:

دانلود نسخه سروری نرم‌افزار رقم


من نسخه اندروید را در https://github.com/ آپلود کردم

عالی. حالا برای ساخت فایل APK این مراحل را انجام دهید:

وارد مخزن پروژه در GitHub شوید.

بالای صفحه روی Actions بزنید.

از ستون سمت چپ، گردش‌کار Build Android APK را انتخاب کنید.

روی Run workflow بزنید و دوباره اجرای آن را تأیید کنید. گردش‌کارهایی که workflow_dispatch دارند از همین بخش دستی اجرا می‌شوند. 

چند دقیقه صبر کنید تا کنار اجرای جدید علامت سبز ظاهر شود.

روی همان اجرای موفق کلیک کنید.

پایین صفحه، بخش Artifacts را پیدا کنید.

فایل ساخته‌شده مانند ragham-android-apk را دانلود کنید. GitHub فایل‌های خروجی گردش‌کار را در بخش Artifacts قرار می‌دهد. 

فایل دانلودشده معمولاً ZIP است؛ آن را استخراج کنید تا فایل APK داخل آن دیده شود.

APK را به گوشی منتقل و نصب کنید.

هنگام اولین اجرا، آدرس نسخه سرور را وارد کنید؛ مثلاً در شبکه داخلی:

http://192.168.1.25:8080
یا برای سرور اینترنتی:

https://your-domain.com
اگر بخش Actions خطا یا علامت قرمز نشان داد، لینک مخزن GitHub یا تصویر صفحه خطا را بفرستید تا دقیق بررسی کنم.


Build Android APK را پیدا نکردم

به‌مدت 1m 1s پردازش کرد
احتمالاً فایل گردش‌کار یک پوشه پایین‌تر قرار گرفته است. در بسته قبلی مسیر آن این‌گونه بود:

ragham_android_app/.github/workflows/build-apk.yml
اما GitHub فقط گردش‌کارهایی را شناسایی می‌کند که مستقیماً در ریشه مخزن و در مسیر زیر باشند: 

.github/workflows/build-apk.yml
راه‌حل سریع در GitHub
وارد مخزن شوید.

روی Add file بزنید.

گزینه Create new file را انتخاب کنید.

در قسمت نام فایل دقیقاً این مسیر را وارد کنید:

.github/workflows/build-apk.yml
فایل آماده زیر را دانلود کنید، محتوای آن را در GitHub کپی کنید و سپس Commit changes را بزنید:

build-apk.yml

مطمئن شوید فایل روی شاخه اصلی، یعنی main یا master ذخیره شده باشد.

صفحه را تازه‌سازی کنید و وارد تب Actions شوید.

در سمت چپ باید Build Android APK ظاهر شود.

آن را انتخاب کنید و روی Run workflow بزنید. اجرای دستی زمانی نمایش داده می‌شود که فایل دارای workflow_dispatch باشد و در شاخه پیش‌فرض مخزن قرار گرفته باشد. 

نسخه اصلاح‌شده مخصوص GitHub
این نسخه طوری بسته‌بندی شده که پوشه .github مستقیماً در ریشه پروژه قرار دارد. ZIP را ابتدا در کامپیوتر استخراج کنید و محتویات داخل آن را در مخزن بارگذاری کنید، نه خود فایل ZIP را:

دانلود پروژه آماده بارگذاری در GitHub

ساختار صحیح مخزن باید تقریباً چنین باشد:

app/
.github/
    workflows/
        build-apk.yml
build.gradle
settings.gradle
gradle.properties
README_FA.md
اگر تب Actions اصلاً دیده نمی‌شود یا غیرفعال است، به مسیر زیر بروید و اجرای Actions را فعال کنید:

Settings → Actions → General → Actions permissions
سپس گزینه اجازه اجرای Actions را انتخاب و ذخیره کنید. GitHub امکان غیرفعال‌کردن Actions در تنظیمات هر مخزن را دارد. 


کتابخانه
/
build-apk.yml


name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK packages
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'

      - name: Build debug APK
        run: gradle assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: ragham-android-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk
توضیح دادن
کتابخانه
/
build-apk.yml


name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK packages
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'

      - name: Build debug APK
        run: gradle assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: ragham-android-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk
